# CLAUDE.md — TokenLedger 프로젝트 헌법

이 파일은 모든 세션에서 반드시 준수해야 하는 프로젝트 규칙이다.

## 0. 사용자 컨텍스트 (가장 중요)

- 나는 **비전공자**다. 모든 설명은 쉬운 한국어로 한다. 전문용어를 쓸 때는 괄호로 한 줄 설명을 붙인다.
- 코드를 작성하기 전에 **무엇을 왜 하려는지 먼저 한국어로 설명하고 내 승인을 받는다.** 승인 없이 대규모 변경을 하지 않는다.
- 내가 이해하지 못한 채 진행되는 코드는 부채다. 각 단계가 끝나면 "브라우저/터미널에서 이렇게 확인하세요"라는 검증 방법을 알려준다.

## 1. 제품 개요

**TokenLedger** — 개인 개발자와 1인 팀을 위한 AI 지출 추적 대시보드 ("AI 비용 가계부").

- 한 줄 정의: "내 AI 에이전트들이 어디에 얼마 썼는지"를 보여주고, 청구서 충격이 오기 전에 알려주는 서비스.
- 타겟: ① Claude Code Pro/Max 구독자 (공식 사용량 분석에서 배제된 집단), ② Claude Code + OpenAI + OpenRouter를 섞어 쓰는 인디 개발자.
- 포지셔닝: 엔터프라이즈용(Langfuse, LiteLLM)이 아니라 **개인용**. 설치 1분, 읽기 전용, 예쁜 대시보드.

## 2. 검증된 페인포인트 (기능의 존재 이유)

레딧(r/ClaudeAI, r/LLMDevs, r/cursor 등) 조사로 확인된 미해결 문제:

1. **소비의 불투명성** — "한 문장 답변에 사용량이 59%→100%로 갔다. 어떻게?"
2. **사후 인지** — 첫 신호가 월말 청구서. 실시간 알림 부재.
3. **낭비 추적 불가** — 루프에 빠진 세션 하나가 몇 분 만에 하루 예산을 소진해도 구분할 방법이 없음.
4. **구독자 사각지대** — /cost 명령과 공식 분석은 Pro/Max 개인 구독자에게 제공되지 않음.
5. **멀티툴 파편화** — 도구마다 사용량이 흩어져 있고 통합 뷰가 없음.

## 3. 기술 스택

| 구성요소 | 기술 | 비고 |
|---|---|---|
| 수집기 CLI | TypeScript, npm 배포 | ccusage 오픈소스 구조 참고 |
| 웹 대시보드 | Next.js (App Router) | |
| DB/인증/실시간 | Supabase | |
| 결제 | Stripe 또는 Lemon Squeezy | v0.2에서 도입 |
| 차트 | recharts | |

## 4. 핵심 설계 원칙 (절대 위반 금지)

- **프라이버시 우선**: 프롬프트 본문·코드 내용은 절대 서버로 전송하지 않는다. 전송 가능한 것은 토큰 수, 모델명, 타임스탬프, 비용, 세션 해시 등 메타데이터뿐이다.
- **읽기 전용**: 사용자의 API 요청 경로에 끼어들지 않는다 (프록시/게이트웨이 방식 금지). 로컬 로그 파일과 공식 usage API만 읽는다.
- **단순함 유지**: 1인이 운영 가능한 범위를 넘는 기능은 만들지 않는다.

## 5. MVP 기능 명세

### F1. 통합 수집기 (페인포인트 ④⑤)
- `npx tokenledger init` 한 줄로 설치.
- v0.1 수집 대상 3개로 제한: Claude Code(로컬 JSONL 파싱), OpenAI(공식 usage API), OpenRouter(공식 API).
- Cursor 연동은 v0.2로 연기 (비공식 API 리스크).

### F2. 드릴다운 대시보드 (페인포인트 ①)
- 계층: 일/주/월 → 도구별 → 프로젝트(리포)별 → 세션별.
- 세션 상세: 입력/출력/캐시 토큰 분해, 예상 비용.
- 화면 구성: 월간 총액 카드, 일별 추이 차트, 도구별 파이차트, 세션 목록 테이블.

### F3. 사전 알림 (페인포인트 ②)
- 일일 예산 임계치 알림 (80% / 100%).
- 급증 감지: 직전 7일 평균 대비 3배 이상 소비 속도.
- 주간 요약 리포트.
- 채널: 이메일 + Discord/Slack 웹훅.

### F4. 낭비 감지 (페인포인트 ③ — 차별화 핵심)
세션별로 4개 신호를 검사해 점수화하고, "이번 주 낭비 추정: $12 (전체의 31%)" 형태의 한 줄 요약을 만든다.

| 신호 | 감지 로직 |
|---|---|
| 루프 의심 | 동일 세션에서 유사 도구 호출이 짧은 간격으로 반복 (호출 시그니처 해싱) |
| 실패 재시도 체인 | 에러 응답 직후 재호출 연쇄 패턴 |
| 컨텍스트 비대 | 입력:출력 비율 극단적 이상치 + 캐시 히트율 급락 |
| 무산출 고비용 | 코드 변경 0줄 + 비용 상위 10% 세션 |

- 구현 순서: 무산출 고비용 → 실패 재시도 → 컨텍스트 비대 → 루프 의심 (쉬운 것부터).
- 목표는 정밀 분석이 아니라 **행동 가능한 힌트**다. 휴리스틱으로 충분하다.

## 6. 만들지 않는 것 (스코프 방어)

- 프록시/게이트웨이 (요청 경로 개입 금지)
- 예산 초과 시 강제 차단
- 팀 권한 관리, 조직 기능
- 프롬프트 최적화 제안
- 엔터프라이즈 기능 전반

누군가(미래의 나 포함) 이 목록의 기능을 요청하면, 먼저 이 섹션을 상기시킨다.

## 7. 작업 규칙

1. **조사 → 보고 → 승인 → 구현** 순서를 지킨다. 코드부터 쓰지 않는다.
2. 한 세션에서는 한 기능만 다룬다.
3. 기능 완료 시마다: 테스트 실행 → 통과 확인 → git 커밋 (메시지는 Claude가 작성).
4. 같은 에러로 2~3회 실패하면 재시도를 멈추고, 시도한 방법과 실패 이유를 정리한 뒤 다른 접근법 2가지를 제안한다.
5. 외부 서비스(Supabase, Stripe 등) 작업 시 "사용자가 웹에서 직접 할 일"과 "Claude가 할 일"을 구분해서 안내한다.
6. 새 라이브러리 추가 전에 이유와 대안을 설명하고 승인받는다.

## 8. 로드맵

| 주차 | 목표 | 상태 |
|---|---|---|
| 1 | 랜딩페이지 + 웨이트리스트, JSONL 파서 프로토타입 | ✅ |
| 2~3 | CLI 수집기 + 기본 대시보드 (F1, F2) | ✅ |
| 4 | 알림 (F3) + 낭비 감지 2종 (F4 일부) | ✅ |
| 5 | 비공개 베타 20명, 피드백 반영 | ⬜ |
| 6 | Product Hunt + r/ClaudeAI 런칭 | ⬜ |

작업이 완료되면 상태 열을 ✅로 갱신한다.

## 9. 수익 모델 (참고)

- 무료: 로컬 CLI + 7일 히스토리.
- Pro $9/월: 무제한 히스토리, 알림, 멀티툴 통합, 낭비 리포트.
- 프레임: "지출의 5%로 지출을 지킨다."
